package com.bianl.readingwhat.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bianl.readingwhat.db.model.StaredMovie;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "STARED_MOVIE".
*/
public class StaredMovieDao extends AbstractDao<StaredMovie, Long> {

    public static final String TABLENAME = "STARED_MOVIE";

    /**
     * Properties of entity StaredMovie.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MovieId = new Property(1, String.class, "movieId", false, "MOVIE_ID");
        public final static Property SmallImage = new Property(2, String.class, "smallImage", false, "SMALL_IMAGE");
        public final static Property Title = new Property(3, String.class, "title", false, "TITLE");
    }


    public StaredMovieDao(DaoConfig config) {
        super(config);
    }
    
    public StaredMovieDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"STARED_MOVIE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"MOVIE_ID\" TEXT," + // 1: movieId
                "\"SMALL_IMAGE\" TEXT," + // 2: smallImage
                "\"TITLE\" TEXT);"); // 3: title
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"STARED_MOVIE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, StaredMovie entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String movieId = entity.getMovieId();
        if (movieId != null) {
            stmt.bindString(2, movieId);
        }
 
        String smallImage = entity.getSmallImage();
        if (smallImage != null) {
            stmt.bindString(3, smallImage);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(4, title);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, StaredMovie entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String movieId = entity.getMovieId();
        if (movieId != null) {
            stmt.bindString(2, movieId);
        }
 
        String smallImage = entity.getSmallImage();
        if (smallImage != null) {
            stmt.bindString(3, smallImage);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(4, title);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public StaredMovie readEntity(Cursor cursor, int offset) {
        StaredMovie entity = new StaredMovie( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // movieId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // smallImage
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // title
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, StaredMovie entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMovieId(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setSmallImage(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTitle(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(StaredMovie entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(StaredMovie entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(StaredMovie entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
